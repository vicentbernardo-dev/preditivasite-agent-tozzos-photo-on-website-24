import {
  COMMENTS_HIGHLIGHT_HUE_KEY,
  applyInlineCommentIdAttr,
  isTextSelectionComment,
  parseCommentFieldPath
} from "./chunk-5XKBWDNS.js";
import {
  DIFF_DELETE,
  DIFF_EQUAL,
  DIFF_INSERT,
  apply,
  cleanupEfficiency,
  diff,
  getValueAtPath,
  make,
  startsWith,
  toPlainText,
  toString
} from "./chunk-KLQT3VE7.js";
import {
  ft,
  gt,
  hues
} from "./chunk-NB2HO54E.js";
import {
  require_compiler_runtime
} from "./chunk-Q25QEBQ2.js";
import {
  require_jsx_runtime,
  require_react
} from "./chunk-FW3TAT2P.js";
import {
  isPortableTextSpan,
  isPortableTextTextBlock
} from "./chunk-VAD7FIHE.js";
import {
  __toESM
} from "./chunk-6LNZPZHA.js";

// ../node_modules/sanity/lib/buildTextSelectionFromFragment-DGgSjC7F.js
var import_react = __toESM(require_react(), 1);
var import_compiler_runtime = __toESM(require_compiler_runtime(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
function CommentDisabledIcon(props) {
  let $ = (0, import_compiler_runtime.c)(8), ref, rest;
  $[0] === props ? (ref = $[1], rest = $[2]) : ({ ref, ...rest } = props, $[0] = props, $[1] = ref, $[2] = rest);
  let t0, t1;
  $[3] === Symbol.for("react.memo_cache_sentinel") ? (t0 = (0, import_jsx_runtime.jsx)("path", {
    d: "M5.92627 14.5381H7.92627V18.5381L11.9263 14.5381H15.9263C17.0308 14.5381 17.9263 13.6427 17.9263 12.5381V6.53809C17.9263 5.43352 17.0308 4.53809 15.9263 4.53809H5.92627C4.8217 4.53809 3.92627 5.43352 3.92627 6.53809V12.5381C3.92627 13.6427 4.8217 14.5381 5.92627 14.5381Z",
    stroke: "currentColor",
    strokeWidth: "1.2",
    strokeLinejoin: "round"
  }), t1 = (0, import_jsx_runtime.jsx)("path", {
    d: "M2.77256 2.66835C2.54413 2.51319 2.23317 2.57259 2.07802 2.80103L1.40378 3.7937C1.32927 3.9034 1.30139 4.0382 1.32627 4.16845C1.35115 4.29871 1.42676 4.41374 1.53646 4.48825L19.5861 16.7479C19.8146 16.903 20.1255 16.8436 20.2807 16.6152L20.9549 15.6225C21.0294 15.5128 21.0573 15.378 21.0324 15.2478C21.0076 15.1175 20.9319 15.0025 20.8222 14.928L2.77256 2.66835Z",
    fill: "currentColor",
    strokeLinejoin: "round"
  }), $[3] = t0, $[4] = t1) : (t0 = $[3], t1 = $[4]);
  let t2;
  return $[5] !== ref || $[6] !== rest ? (t2 = (0, import_jsx_runtime.jsxs)("svg", {
    "data-sanity-icon": "comment-disabled",
    width: "1em",
    height: "1em",
    ref,
    viewBox: "0 0 25 25",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    ...rest,
    children: [t0, t1]
  }), $[5] = ref, $[6] = rest, $[7] = t2) : t2 = $[7], t2;
}
var HighlightSpan = gt.span.withConfig({
  displayName: "HighlightSpan",
  componentId: "sc-8rkq2u-0"
})(({ theme }) => {
  var _a;
  let isDark = (_a = theme.sanity.v2) == null ? void 0 : _a.color._dark, addedBg = hues[COMMENTS_HIGHLIGHT_HUE_KEY][isDark ? 800 : 100].hex, addedBorder = hues[COMMENTS_HIGHLIGHT_HUE_KEY][isDark ? 700 : 300].hex, addedHoverBg = hues[COMMENTS_HIGHLIGHT_HUE_KEY][isDark ? 700 : 200].hex, addedHoverBorder = hues[COMMENTS_HIGHLIGHT_HUE_KEY][isDark ? 600 : 400].hex, addedNestedBg = hues[COMMENTS_HIGHLIGHT_HUE_KEY][isDark ? 700 : 200].hex, addedNesterBorder = hues[COMMENTS_HIGHLIGHT_HUE_KEY][isDark ? 600 : 400].hex;
  return ft`box-sizing:border-box;transition:background-color 100ms ease,border-color 100ms ease;*{mix-blend-mode:${isDark ? "screen" : "multiply"};}&[data-inline-comment-state='added'][data-inline-comment-nested='false']{background-color:${addedBg};border-bottom:2px solid ${addedBorder};}&[data-inline-comment-state='added'][data-inline-comment-nested='true']{background-color:${addedNestedBg};border-bottom:2px solid ${addedNesterBorder};}&[data-inline-comment-state='added'][data-inline-comment-nested='false'][data-hovered='true']{background-color:${addedHoverBg};border-bottom:2px solid ${addedHoverBorder};}&[data-inline-comment-state='authoring']{background-color:${addedBg};border-bottom:2px solid ${addedBorder};}`;
});
function CommentInlineHighlightSpan(props) {
  let $ = (0, import_compiler_runtime.c)(15), children, isAdded, isAuthoring, isHovered, isNested, ref, rest;
  $[0] === props ? (children = $[1], isAdded = $[2], isAuthoring = $[3], isHovered = $[4], isNested = $[5], ref = $[6], rest = $[7]) : ({ ref, children, isAdded, isAuthoring, isHovered, isNested, ...rest } = props, $[0] = props, $[1] = children, $[2] = isAdded, $[3] = isAuthoring, $[4] = isHovered, $[5] = isNested, $[6] = ref, $[7] = rest);
  let state = isAdded ? "added" : isAuthoring ? "authoring" : void 0, t0 = isHovered ? "true" : "false", t1 = isNested ? "true" : "false", t2;
  return $[8] !== children || $[9] !== ref || $[10] !== rest || $[11] !== state || $[12] !== t0 || $[13] !== t1 ? (t2 = (0, import_jsx_runtime.jsx)(HighlightSpan, {
    ...rest,
    "data-hovered": t0,
    "data-inline-comment-nested": t1,
    "data-inline-comment-state": state,
    ref,
    children
  }), $[8] = children, $[9] = ref, $[10] = rest, $[11] = state, $[12] = t0, $[13] = t1, $[14] = t2) : t2 = $[14], t2;
}
function diffText(current, next) {
  let diff2 = diff(current, next), diffs = cleanupEfficiency(diff2), levenshtein = diffsLevenshtein(diffs);
  return {
    patches: make(current, diffs, { margin: 15 }),
    levenshtein
  };
}
function diffApply(current, patches) {
  return apply(patches, current, {
    allowExceedingIndices: true,
    margin: 15
  })[0];
}
function toPlainTextWithChildSeparators(inputBlock) {
  return inputBlock.children.map((child) => isPortableTextSpan(child) ? child.text.replaceAll("", " ") : "").join("");
}
var COMMENT_INDICATORS = ["", ""];
var COMMENT_INDICATORS_REGEX = RegExp(`[${COMMENT_INDICATORS.join("")}]`, "g");
var EMPTY_ARRAY = [];
function buildRangeDecorationSelectionsFromComments(props) {
  let { value, comments, documentValue, basePath } = props;
  if (!value || value.length === 0) return EMPTY_ARRAY;
  let textSelections = comments.filter(isTextSelectionComment), decorators = [];
  return textSelections.forEach((comment) => {
    var _a, _b, _c;
    let relative = [];
    if (documentValue !== void 0 && basePath) {
      let fieldPath = parseCommentFieldPath((_a = comment.target.path) == null ? void 0 : _a.field);
      if (!fieldPath || !startsWith(basePath, fieldPath)) return;
      relative = fieldPath.slice(basePath.length);
    }
    (_c = (_b = comment.target.path) == null ? void 0 : _b.selection) == null ? void 0 : _c.value.forEach((selectionMember) => {
      let matchedBlock = documentValue !== void 0 && basePath ? getValueAtPath(documentValue, [
        ...basePath,
        ...relative,
        { _key: selectionMember._key }
      ]) : value.find((block) => block._key === selectionMember._key);
      if (!matchedBlock || !isPortableTextTextBlock(matchedBlock)) return;
      let selectionText = selectionMember.text.replaceAll(COMMENT_INDICATORS_REGEX, ""), textWithChildSeparators = toPlainTextWithChildSeparators(matchedBlock), { patches } = diffText(selectionText, selectionMember.text), diffedText = diffApply(textWithChildSeparators, patches), startIndex = diffedText.indexOf(COMMENT_INDICATORS[0]), endIndex = diffedText.replaceAll(COMMENT_INDICATORS[0], "").indexOf(COMMENT_INDICATORS[1]), textWithoutCommentTags = diffedText.replaceAll(COMMENT_INDICATORS_REGEX, ""), oldCommentedText = selectionMember.text.slice(selectionMember.text.indexOf(COMMENT_INDICATORS[0]) + 1, selectionMember.text.indexOf(COMMENT_INDICATORS[1])), newCommentedText = textWithoutCommentTags.slice(startIndex, endIndex), { levenshtein } = diffText(newCommentedText, oldCommentedText), threshold = Math.round(newCommentedText.length + oldCommentedText.length / 2), nullSelection = false;
      if (newCommentedText.length === 0 && (nullSelection = true), levenshtein > threshold && (nullSelection = true), startIndex + 1 === endIndex && (nullSelection = true), startIndex !== -1 && endIndex !== -1) {
        let childIndexAnchor = 0, anchorOffset = 0, childIndexFocus = 0, focusOffset = 0;
        for (let i = 0; i < textWithoutCommentTags.length && (textWithoutCommentTags[i] === "" && (i <= startIndex && (anchorOffset = -1, childIndexAnchor++), focusOffset = -1, childIndexFocus++), i < startIndex && anchorOffset++, i < startIndex + newCommentedText.length && focusOffset++, i !== startIndex + newCommentedText.length); i++) ;
        decorators.push({
          selection: {
            anchor: {
              path: [
                ...relative,
                { _key: matchedBlock._key },
                "children",
                { _key: matchedBlock.children[childIndexAnchor]._key }
              ],
              offset: anchorOffset
            },
            focus: {
              path: [
                ...relative,
                { _key: matchedBlock._key },
                "children",
                { _key: matchedBlock.children[childIndexFocus]._key }
              ],
              offset: focusOffset
            }
          },
          comment,
          range: {
            _key: matchedBlock._key,
            text: nullSelection ? "" : diffedText
          }
        });
      }
    });
  }), decorators.length === 0 ? EMPTY_ARRAY : decorators;
}
function diffsLevenshtein(diffs) {
  let levenshtein = 0, insertions = 0, deletions = 0;
  for (let x = 0; x < diffs.length; x++) {
    let op = diffs[x][0], data = diffs[x][1];
    switch (op) {
      case DIFF_INSERT:
        insertions += data.length;
        break;
      case DIFF_DELETE:
        deletions += data.length;
        break;
      case DIFF_EQUAL:
        levenshtein += Math.max(insertions, deletions), insertions = 0, deletions = 0;
    }
  }
  return levenshtein += Math.max(insertions, deletions), levenshtein;
}
var CommentRangeDecoration = (0, import_react.memo)(function CommentRangeDecoration2(props) {
  let $ = (0, import_compiler_runtime.c)(20), { children, commentId, currentHoveredCommentId, onClick, onHoverEnd, onHoverStart, selectedThreadId, threadId } = props, decoratorRef = (0, import_react.useRef)(null), [isNested, setIsNested] = (0, import_react.useState)(false), [parentCommentId, setParentCommentId] = (0, import_react.useState)(null), t0, t1;
  $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = () => {
    var _a, _b;
    let prevEl = (_a = decoratorRef.current) == null ? void 0 : _a.previousSibling, nextEl = (_b = decoratorRef.current) == null ? void 0 : _b.nextSibling;
    if (!prevEl || !nextEl) {
      setIsNested(false);
      return;
    }
    let [key] = Object.keys(applyInlineCommentIdAttr("")), prevId = prevEl.getAttribute(key), nextId = nextEl.getAttribute(key), isNestedDecorator = !!(prevId && nextId && prevId === nextId);
    setParentCommentId(isNestedDecorator ? prevId : null), setIsNested(isNestedDecorator);
  }, t1 = [], $[0] = t0, $[1] = t1) : (t0 = $[0], t1 = $[1]), (0, import_react.useEffect)(t0, t1);
  let t2;
  $[2] !== commentId || $[3] !== onHoverStart ? (t2 = () => onHoverStart(commentId), $[2] = commentId, $[3] = onHoverStart, $[4] = t2) : t2 = $[4];
  let handleMouseEnter = t2, t3;
  $[5] === onHoverEnd ? t3 = $[6] : (t3 = () => onHoverEnd(null), $[5] = onHoverEnd, $[6] = t3);
  let handleMouseLeave = t3, t4;
  $[7] !== commentId || $[8] !== onClick ? (t4 = () => onClick(commentId), $[7] = commentId, $[8] = onClick, $[9] = t4) : t4 = $[9];
  let handleClick = t4, t5 = currentHoveredCommentId === commentId || currentHoveredCommentId === parentCommentId && isNested || selectedThreadId === threadId, t6;
  $[10] === threadId ? t6 = $[11] : (t6 = applyInlineCommentIdAttr(threadId), $[10] = threadId, $[11] = t6);
  let t7;
  return $[12] !== children || $[13] !== handleClick || $[14] !== handleMouseEnter || $[15] !== handleMouseLeave || $[16] !== isNested || $[17] !== t5 || $[18] !== t6 ? (t7 = (0, import_jsx_runtime.jsx)(CommentInlineHighlightSpan, {
    isAdded: true,
    isHovered: t5,
    isNested,
    onClick: handleClick,
    onMouseEnter: handleMouseEnter,
    onMouseLeave: handleMouseLeave,
    ref: decoratorRef,
    ...t6,
    children
  }), $[12] = children, $[13] = handleClick, $[14] = handleMouseEnter, $[15] = handleMouseLeave, $[16] = isNested, $[17] = t5, $[18] = t6, $[19] = t7) : t7 = $[19], t7;
});
function buildCommentRangeDecorations(props) {
  let { comments, currentHoveredCommentId, onDecorationClick, onDecorationHoverEnd, onDecorationHoverStart, onDecorationMoved, selectedThreadId, value, documentValue, basePath } = props;
  return buildRangeDecorationSelectionsFromComments({
    comments,
    value,
    documentValue,
    basePath
  }).map(({ selection, comment, range }) => ({
    component: ({ children }) => (0, import_jsx_runtime.jsx)(CommentRangeDecoration, {
      commentId: comment._id,
      currentHoveredCommentId,
      onClick: onDecorationClick,
      onHoverEnd: onDecorationHoverEnd,
      onHoverStart: onDecorationHoverStart,
      selectedThreadId,
      threadId: comment.threadId,
      children
    }),
    onMoved: onDecorationMoved,
    selection,
    payload: {
      commentId: comment._id,
      range
    }
  }));
}
function buildTextSelectionFromFragment(props) {
  let { fragment, documentValue, basePath, selection } = props;
  if (!selection) throw Error("Selection is required");
  let normalizedSelection = selection.backward ? {
    backward: false,
    anchor: selection.focus,
    focus: selection.anchor
  } : selection, { block: anchorBlock, path: anchorBlockPath } = findEnclosingTextBlock(documentValue, basePath, normalizedSelection.anchor.path) ?? {}, { block: focusBlock, path: focusBlockPath } = findEnclosingTextBlock(documentValue, basePath, normalizedSelection.focus.path) ?? {}, containingArrays = [];
  for (let blockPath of [anchorBlockPath, focusBlockPath]) {
    if (!blockPath) continue;
    let containingArray = getValueAtPath(documentValue, [...basePath, ...blockPath.slice(0, -1)]);
    Array.isArray(containingArray) && !containingArrays.includes(containingArray) && containingArrays.push(containingArray);
  }
  return {
    type: "text",
    value: fragment.map((fragmentBlock) => {
      let originalBlock = findBlockByKey(containingArrays, fragmentBlock._key);
      if (!isPortableTextTextBlock(originalBlock)) return {
        _key: fragmentBlock._key,
        text: ""
      };
      let anchorBlockKey = anchorBlock == null ? void 0 : anchorBlock._key, focusBlockKey = focusBlock == null ? void 0 : focusBlock._key, fragmentBlockText = toPlainText([fragmentBlock]), fragmentStartSpan = isPortableTextTextBlock(fragmentBlock) ? fragmentBlock.children[0] : void 0, fragmentEndSpan = isPortableTextTextBlock(fragmentBlock) ? fragmentBlock.children[fragmentBlock.children.length - 1] : void 0, originalTextBeforeSelection = "", startChildIndex = -1;
      if (anchorBlockKey === originalBlock._key) for (let child of originalBlock.children) {
        if (startChildIndex++, child._key === (fragmentStartSpan == null ? void 0 : fragmentStartSpan._key)) {
          originalTextBeforeSelection += isPortableTextSpan(child) && child.text.slice(0, Math.max(0, normalizedSelection.anchor.offset)) || "";
          break;
        }
        originalTextBeforeSelection += child.text;
      }
      let originalTextAfterSelection = "";
      if (focusBlockKey === originalBlock._key) for (let child of originalBlock.children.slice(startChildIndex).reverse()) {
        if (child._key === (fragmentEndSpan == null ? void 0 : fragmentEndSpan._key)) {
          originalTextAfterSelection = (isPortableTextSpan(child) && child.text.slice(normalizedSelection.focus.offset, child.text.length) || "") + originalTextAfterSelection;
          break;
        }
        originalTextAfterSelection = child.text + originalTextAfterSelection;
      }
      return {
        _key: originalBlock._key,
        text: `${originalTextBeforeSelection}${COMMENT_INDICATORS[0]}${fragmentBlockText}${COMMENT_INDICATORS[1]}${originalTextAfterSelection}`
      };
    })
  };
}
function getCommentFieldPath(documentValue, basePath, selectionPath) {
  var _a;
  let enclosingBlockPath = (_a = findEnclosingTextBlock(documentValue, basePath, selectionPath)) == null ? void 0 : _a.path;
  if (enclosingBlockPath) return toString([...basePath, ...enclosingBlockPath.slice(0, -1)]);
}
function findEnclosingTextBlock(documentValue, basePath, path) {
  for (let end = path.length; end > 0; end--) {
    let relative = path.slice(0, end), candidate = getValueAtPath(documentValue, [...basePath, ...relative]);
    if (isPortableTextTextBlock(candidate)) return {
      block: candidate,
      path: relative
    };
  }
}
function findBlockByKey(arrays, key) {
  for (let array of arrays) for (let block of array) if (isPortableTextTextBlock(block) && block._key === key) return block;
}

export {
  CommentDisabledIcon,
  CommentInlineHighlightSpan,
  buildRangeDecorationSelectionsFromComments,
  buildCommentRangeDecorations,
  buildTextSelectionFromFragment,
  getCommentFieldPath
};
//# sourceMappingURL=chunk-4ZAH7L54.js.map
